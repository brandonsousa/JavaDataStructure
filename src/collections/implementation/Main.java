package collections.implementation;

import collections.classes.Person;

import java.util.ArrayList;
import java.util.Collections;
import java.util.Scanner;

public class Main {

    public static void main(String[] args){

        /*Scanner in = new Scanner(System.in);

        ArrayList<Person> personArrayList = new ArrayList<Person>();

        System.out.println("how many people do you want to register?");
        int size = in.nextInt();

        for (int i = 0; i < size; i++){

            Person person = new Person();

            System.out.println("Insert name");
            person.setName(in.next());

            System.out.println("Insert note to " + person.getName());
            person.setNote(in.nextInt());

            personArrayList.add(person);
        }

        Collections.sort(personArrayList);
        for (int i = 0; i < size; i++){
            System.out.println("Name: "+ personArrayList.get(i).getName()
                    + " | Note " + personArrayList.get(i).getNote());
        }*/

        /*ArrayList<Integer> a = new ArrayList<Integer>();
        long in = System.currentTimeMillis();
        for (int i = 0; i < 300000; i++){
            a.add(i);
        }

        for (int i = 0; i < 300000; i++){
            a.contains(i);
        }

        long f = System.currentTimeMillis();
        System.out.printf("%.3f segundos%n", (in - f)/10000d);*/


    }

}
